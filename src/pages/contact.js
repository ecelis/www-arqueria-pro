import React from "react";
import Layout from "../components/layout";

const Contact = (props) => {
    return (
        <Layout>
        <div>
            <p>Queremos escucharte</p>
            <a href="mailto:hola@arqueria.pro">hola@arqueria.pro</a>
        </div>
        </Layout>
    );
};

export default Contact;
